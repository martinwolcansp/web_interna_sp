/* ============================================================
   sector-comunicaciones-eje2.js — Arranque de la página del módulo
   Procesos (Eje 2) del Sector Comunicaciones.
   Página de un solo módulo: se renderiza directo al cargar.
   ============================================================ */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
  renderEjeById('eje2-procesos');
});
