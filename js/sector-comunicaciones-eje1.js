/* ============================================================
   sector-comunicaciones-eje1.js — Arranque de la página del módulo
   Unidad de Negocio (Eje 1) del Sector Comunicaciones.
   Página de un solo módulo: se renderiza directo al cargar.
   ============================================================ */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
  renderEjeById('eje1-unidad-negocio');
});
